# ───────── Core ORM & DB ─────────
SQLAlchemy>=2.0,<2.1
alembic>=1.13.0         # DB migrations

# ───────── Auth & Security ─────────
passlib[bcrypt]>=1.7
python-jose[cryptography]>=3.3.0   # JWT auth
bcrypt>=4.2.0

# ───────── GUI & Calendar ─────────
tkcalendar>=1.6
pillow>=10.0.0          # for icons, images

# ───────── API & Chatbot ─────────
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.6
requests>=2.31
httpx>=0.27.0           # async HTTP calls (better than requests for some cases)

# ───────── Date & Time Parsing ─────────
python-dateutil>=2.9
dateparser>=1.2

# ───────── Fuzzy Matching (Doctor Name, Search, NLP Helpers) ─────────
rapidfuzz>=3.0

# ───────── PDF & Export ─────────
reportlab>=4.0.8

# ───────── AI / LLM Inference (Local TinyLlama Support) ─────────
llama-cpp-python>=0.2.79   # for tinyllama.gguf local inference

# ───────── Config & Utilities ─────────
python-dotenv>=1.0.1
typing-extensions>=4.5.0

# ───────── Dev/Optional ─────────
rich>=13.8.1             # pretty logs
loguru>=0.7.2            # structured logging
pytest>=8.3.3
pytest-asyncio>=0.23.8
